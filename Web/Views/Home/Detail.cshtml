@model VacancyLocationModel
@{
    ViewData["Title"] = "Vacancy";
}

<div>
    <div id="vacancies">
        <div class="text-centre" id="loading">
            <div class="loader" v-if="isLoading"></div>
        </div>

        <h2>@Model.Title</h2>
        <h4>@Model.LocationName</h4>
        <p>@Model.Description</p>
        <p>@Model.PostedDate</p>
    </div>
</div>

